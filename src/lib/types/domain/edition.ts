export type edition = {
	year: number
	songs: string[] // List of references to song path
	users: string[] // List of references to user path
	concertDate?: string
	ticketLink?: string
	auditionLink?: string
}
